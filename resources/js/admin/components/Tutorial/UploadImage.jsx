import React from "react";

export default function UploadImage() {
    return (
        <span>
            Bisa di gunakan untuk upload gambar (.png/.jpg) / file dokumen seperti (.pdf/.docx/.pptx)
        </span>
    );
}
